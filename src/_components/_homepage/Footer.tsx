import React, { useEffect, useState } from "react";

interface IFooterProps {
  test?: any
}

export const Footer: React.FC<IFooterProps> = (props): JSX.Element | null => {


  return (
    <div>

    </div>
  );
};
